function calculates(text) {
  //transformTextToNumber(text);
  return transformTextToNumber(text);
}

function transformTextToNumber(text) {
  var convertedNumber = parseInt(text, 10);
  return convertedNumber;
}